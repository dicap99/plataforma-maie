// Acceso a datos (PostgreSQL) — RF-ADM-03 — Gestión financiera: transferencias y contratos OPS
// Tablas: transferencias, contratos_ops
// Consultas parametrizadas con require('../../../config/db').query(sql, params).

module.exports = {};
