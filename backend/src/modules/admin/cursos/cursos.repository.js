// Acceso a datos (PostgreSQL) — RF-ADM-01 — Gestión académica: cursos dictados
// Tablas: cursos, curso_estudiantes, modulos_curriculares
// Consultas parametrizadas con require('../../../config/db').query(sql, params).

module.exports = {};
