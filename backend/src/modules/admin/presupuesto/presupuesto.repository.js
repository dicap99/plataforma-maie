// Acceso a datos (PostgreSQL) — RF-ADM-03 — Gestión financiera: presupuesto por cohorte
// Tablas: presupuesto_items
// Consultas parametrizadas con require('../../../config/db').query(sql, params).

module.exports = {};
