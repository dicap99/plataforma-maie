// Acceso a datos (PostgreSQL) — RF-ADM-02 — Plantillas de importación .xlsx
// Sin tablas propias: las plantillas se generan en memoria con exceljs.
// Consultas parametrizadas con require('../../../config/db').query(sql, params).

module.exports = {};
