// Acceso a datos (PostgreSQL) — RF-ADM-04 — Investigación: productos y pasantías
// Tablas: productos_investigacion, pasantias_estancias
// Consultas parametrizadas con require('../../../config/db').query(sql, params).

module.exports = {};
