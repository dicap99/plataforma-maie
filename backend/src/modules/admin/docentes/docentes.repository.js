// Acceso a datos (PostgreSQL) — RF-ADM-06 — Perfil histórico del docente
// Tablas: docentes_perfil, docentes_formacion, cursos
// Consultas parametrizadas con require('../../../config/db').query(sql, params).

module.exports = {};
