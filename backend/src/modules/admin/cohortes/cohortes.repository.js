// Acceso a datos (PostgreSQL) — RF-ADM-01 — Gestión académica: cohortes
// Tablas: cohortes, cohorte_estudiantes
// Consultas parametrizadas con require('../../../config/db').query(sql, params).

module.exports = {};
