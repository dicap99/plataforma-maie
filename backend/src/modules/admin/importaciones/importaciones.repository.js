// Acceso a datos (PostgreSQL) — RF-ADM-01 — Importación masiva CSV/Excel
// Tablas: cohortes, cursos, usuarios, presupuesto_items
// Consultas parametrizadas con require('../../../config/db').query(sql, params).

module.exports = {};
