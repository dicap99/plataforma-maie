// Acceso a datos (PostgreSQL) — RF-ADM-05 — Reportes estadísticos administrativos
// Tablas: cohortes, cursos, presupuesto_items, productos_investigacion
// Consultas parametrizadas con require('../../../config/db').query(sql, params).

module.exports = {};
