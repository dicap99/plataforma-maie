// Acceso a datos (PostgreSQL) — RNF-SEG-01/02 — Autenticación JWT y credenciales bcrypt
// Tablas: usuarios
// Consultas parametrizadas con require('../../config/db').query(sql, params).

module.exports = {};
