// Acceso a datos (PostgreSQL) — RF-RA-02 — Registro de calificaciones por rúbrica
// Tablas: evaluaciones_ra_estudiante, curso_estudiantes
// Consultas parametrizadas con require('../../../config/db').query(sql, params).

module.exports = {};
